name: flutter_carrot_market_d5354
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7 # 벡터 형식 이미지 (크기가 바뀌어도 변하지 않음)

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets: # 이미지 관련 폴더 추가
    - assets/svg/
    - assets/images
